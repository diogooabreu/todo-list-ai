{
  "name": "todo-list-ai",
  "private": true,
  "scripts": {
    "start:dev": "npm run start:dev --prefix apps/backend",
    "build": "npm run build --prefix apps/backend",
    "test": "npm run test --prefix apps/backend",
    "lint": "npm run lint --prefix apps/backend"
  }
}
